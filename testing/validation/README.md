# Manual validation packages

This directory contains reproducible validation packages that complement the
regular pytest suite when a test requires a particular HPC or MPI layout.

- [`rixs_mpi`](rixs_mpi/README.md) validates spinor RIXS with 1, 2, and 4 MPI
  ranks in pure k-point and shared-k-point-subgroup layouts.
